# Testing

Run the library tests:

```bash
moon test
```

Run the executable example:

```bash
moon run cmd/main
```

The tests parse HAR JSON, map typed request and response fields, validate
diagnostics, compute performance summaries, and render text/JSON reports.
